/**
 * shared/storage.ts —— 类型安全的 chrome.storage 读写封装
 *
 * popup / options / background 通过此模块读写持久化数据，
 * 不直接调用 chrome.storage.local.get/set。
 *
 * 首版为脚手架占位，后续逐步实现。
 */

// 占位：后续实现 getConfig / setConfig 等封装函数
// import type { StorageSchema } from "./types";
export {};
